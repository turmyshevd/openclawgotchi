# ===================
# OpenClawGotchi Configuration
# ===================
# Copy this file to .env and fill in your values

# Telegram Bot Token (from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Allowed Telegram User IDs (comma-separated)
# Find your ID: message @userinfobot on Telegram
ALLOWED_USERS=123456789

# Allowed Group IDs (comma-separated, optional)
# Group IDs are negative numbers
ALLOWED_GROUPS=-1001234567890

# Group chat ID for heartbeat messages (optional)
# If not set, heartbeat won't post to groups
GROUP_CHAT_ID=0

# ===================
# Bot Identity (defaults, customized during onboarding)
# ===================

# Bot's display name (shown on E-Ink, used in code)
BOT_NAME=Gotchi

# Owner's name (used in templates)
OWNER_NAME=Owner

# Sibling bot name for inter-bot mail (optional, leave empty if none)
SIBLING_BOT_NAME=

# ===================
# Bot Behavior
# ===================

# Default response language (en, ru, es, etc.)
# Bot will respond in this language unless user writes in another
BOT_LANGUAGE=en

# Claude CLI timeout in seconds (Pi is slow, default 600)
CLAUDE_TIMEOUT=600

# Conversation history limit (messages to keep, default 10)
HISTORY_LIMIT=10

# ===================
# LiteLLM (Lite mode — default backend)
# ===================
# Default preset: "glm" (Z.ai) or "gemini" (Google)
DEFAULT_LITE_PRESET=glm

# If DEFAULT_LITE_PRESET=gemini:
# GEMINI_API_KEY=your_key_from_aistudio.google.com
# GEMINI_MODEL=gemini/gemini-2.0-flash

# If DEFAULT_LITE_PRESET=glm (Z.ai): set ANTHROPIC_API_KEY or Z.ai key as required by your provider

# Optional: custom API base URL (for Z.ai or other proxies)
# GEMINI_API_BASE=

# Model context window size in tokens (default 128000)
# MODEL_CONTEXT_TOKENS=128000

# Active skills to load (comma-separated folder names from openclaw-skills/)
# To save memory on Pi Zero, only load what you need.
# Core skills (coding, display) are always loaded.
ACTIVE_SKILLS=weather,local-places,summarize

# ===================
# Optional: Discord Integration
# ===================
# Webhook (easy, send-only): Create at Discord Channel Settings → Integrations → Webhooks
# DISCORD_WEBHOOK=https://discord.com/api/webhooks/xxx/yyy

# Full bot (advanced, requires discord.py): Create at discord.com/developers
# DISCORD_BOT_TOKEN=your_bot_token
# DISCORD_CHANNEL_ID=123456789

# ===================
# Optional: OpenAI
# ===================
# OPENAI_API_KEY=your_openai_api_key_here
